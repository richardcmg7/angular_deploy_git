language: node_js
node_js:
  - 'node'

branches:
  only:
  - master

addons:
  chrome: stable

before_script:
  - yarn global add @angular/cli

cache:
  yarn: true
  directories:
    - node_modules

script:
  - ng build --prod

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $1d2152b46c5d2230f4d079cc06a45f1dfc8023f6
  keep-history: true
  local-dir: ./dist/
  on:


